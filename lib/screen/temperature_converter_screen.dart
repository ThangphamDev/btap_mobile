import 'package:flutter/material.dart';

class TemperatureConverterScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Chuyển đổi nhiệt độ'));
  }
}
